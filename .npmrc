# Shared (npm + pnpm) — keep only keys BOTH understand here, so npm/npx don't
# warn. pnpm-specific hardening (frozen-lockfile preference, deps verification,
# the 5-day cooldown, build-script allow-list) lives in pnpm-workspace.yaml.
engine-strict=true
