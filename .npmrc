# Reproducible, hardened installs.
engine-strict=true
# CI uses --frozen-lockfile; locally allow lockfile updates.
prefer-frozen-lockfile=true
# Fail the install if the integrity/store check doesn't match.
verify-deps-before-run=true
# Don't auto-run dependency build scripts (defense-in-depth; see onlyBuiltDependencies).
