import { ResolutionCenter } from "@/components/organisms/resolution/resolution-center";

export const metadata = { title: "Resolution Center · Kaseya" };

/** The signature surface: issues grouped by category with fix-classification. */
export default function ResolutionPage() {
  return <ResolutionCenter />;
}
